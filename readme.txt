It was encountered a number of times that
in data acquisition we want the user to define some area on a plot.

Here we implement it in a GUI/Class structure. 

These folders contain clean,short and working examples of having a pyqtgraph plot 
embedded in a GUI, either embedded in the Mainwindow screen or as a summoned dialog
screen.